---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PostList from '../../components/PostList.astro';
import { categoryMap, getPostsByCategory, getPublicPosts } from '../../lib/posts';

const posts = await getPublicPosts();
const category = categoryMap.thoughts;
const categoryPosts = getPostsByCategory(posts, 'thoughts');
---

<BaseLayout title={category.label} description={category.description}>
	<section class="page-shell category-page" data-category="thoughts" aria-labelledby="thoughts-title">
		<p class="page-kicker">Loose Thoughts</p>
		<h1 id="thoughts-title" class="page-title">{category.label}</h1>
		<p class="page-description">{category.description}</p>
		<PostList posts={categoryPosts} />
	</section>
</BaseLayout>
